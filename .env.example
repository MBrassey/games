# --- Auth.js (NextAuth v5) ---
# Generate: openssl rand -base64 32
AUTH_SECRET=

# GitHub OAuth app "games" — https://github.com/settings/developers
AUTH_GITHUB_ID=Ov23liwRyQruNmiCoPKc
AUTH_GITHUB_SECRET=

# Auth.js URL (Vercel sets VERCEL_URL automatically; override for custom domain)
AUTH_URL=http://localhost:3000
# In prod on Vercel: AUTH_URL=https://games.brassey.io

# --- Vercel Postgres (auto-injected on Vercel, fill for local) ---
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# --- Vercel KV (auto-injected on Vercel, fill for local) ---
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
